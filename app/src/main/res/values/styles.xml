<resources>

    <!-- Base application theme. -->
    <!-- Light/Day Mode theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#fff222</item>
        <item name="colorPrimaryDark">#fff342</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="background_color">#ffffff</item>
        <item name="text_color">#000000</item>
        <item name="button_background_color">#ffffff</item>
        <item name="android:windowAnimationStyle">@style/windowTransitionAnimation</item>
    </style>

    <!-- Night/Dark Mode theme. -->
    <style name="darkTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#333132</item>
        <item name="colorPrimaryDark">#333132</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="background_color">#000000</item>
        <item name="text_color">#ffffff</item>
        <item name="button_background_color">#000000</item>
        <item name="android:windowAnimationStyle">@style/windowTransitionAnimation</item>
    </style>

    <style name="windowTransitionAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>



</resources>
